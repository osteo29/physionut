# Database Folder Status

This folder is currently a placeholder and is not the active source of truth for database changes.

Right now, active Supabase schema, migrations, seed files, and SQL editor chunks live under:

- `D:\gpa calc\physio\physionut-main\supabase`

If we keep the current structure, new database work should go in `supabase/` instead of this folder.

If we do a future cleanup, we should choose one of these directions:

1. Remove this folder entirely.
2. Move all database assets here and make it the only database workspace.
